%table{:width => '100%', :border => 0, :cellpadding => 0, :cellspacing => 0, :style => 'background-color:#FFF;font:12px Verdana,Arial,Helvetica,sans-serif;color:#333'}
  %tbody
    %tr
      %td{:style => 'border-bottom:1px solid #AAA;padding:12px'}= link_to image_tag(root_url + 'assets/logo_print.png', :alt => 'SickSick', :border => 0), root_url, :target => '_blank'

    %tr
      %td{:style => 'word-wrap:break-word;padding:8px 12px;margin:0'}
        %p== Olá #{@resource.nome},

        %p{:style => 'font-weight:bold;color:#B60000;font-size:14px'} Foi feita uma requisição para cadastrar uma nova senha.

        %p
          Por favor,
          = link_to 'clique aqui', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)
          para redefinir a sua senha, ou utilize esta url:
          = edit_password_url(@resource, :reset_password_token => @resource.reset_password_token)

        %p Se você não fez esta requisição, por favor, ignore este e-mail.

        %p A sua senha não mudará até que você acesse o link acima e cadastre uma nova senha.

        %br/
        %p
          Atenciosamente,
          %br/
          = link_to configatron.app_name, root_url
